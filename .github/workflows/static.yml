on: push

jobs:
  echo-input:
    runs-on: ubuntu-latest
    steps:
      - run: |
          export PASS="${{ secrets.PASS }}"
          echo $PASS
          echo $FIRST_NAME
        env:
          FIRST_NAME: ${{ secrets.USERNAME }}
          LAST_NAME: Octocat
